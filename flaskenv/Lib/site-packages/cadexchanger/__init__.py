import os
from pathlib import Path


# Necessary for the universal reader to work.
aRoot = os.path.abspath(os.path.dirname(Path(__file__).resolve()))
os.add_dll_directory(f"{aRoot}/bin")
os.environ['PATH']=f"{aRoot}/bin;{os.environ['PATH']}"
